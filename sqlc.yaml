version: "2"
sql:
  - engine: "postgresql"
    schema: "db/migrations"
    queries: "db/queries.sql"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_db_tags: true
